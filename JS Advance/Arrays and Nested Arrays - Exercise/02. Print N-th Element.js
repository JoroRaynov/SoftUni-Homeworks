function print(array, step){
    let arr = [];
    for(let i = 0; i < array.length; i+=step){
    arr.push(array[i]);
    }
    return arr;
}
print(['dsa',
'asd', 
'test', 
'tset'], 
2

);