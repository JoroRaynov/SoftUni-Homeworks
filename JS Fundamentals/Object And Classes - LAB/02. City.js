function person(firstName, lastName, age) {

    let persons = {
        firstName,
        lastName,
        age
    };
return persons;
}
let obj1 =

console.log(person('Peter', 'Pan', '20'));