function sumFirsAndLasArrayElements(arr){

    let firstSum = arr[0];
    let secondSum = arr[arr.length - 1];
    let result = firstSum + secondSum;
    console.log(result);

}
sumFirsAndLasArrayElements(AbortController[11, 58, 69])